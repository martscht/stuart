devtools::load_all('~/stuart/stuart')
data(fairplayer)
dats <- fairplayer[83:92]
dats <- as.data.frame(lapply(dats, as.ordered))
fs <- list(si = names(dats))
for (i in 1:ncol(dats)) {
dats[, i] <- car::recode(dats[, i], '1 = 3; 2 = 3')
}
sel <- mmas(dats, fs, 4,
seed = 55635, cores = 1,
pbest = .25)
data(fairplayer)
dats <- fairplayer[83:92]
fs <- list(si = names(dats))
for (i in 1:ncol(dats)) {
dats[, i] <- car::recode(dats[, i], '1 = 3; 2 = 3')
}
dats <- as.data.frame(lapply(dats, as.ordered))
sel <- mmas(dats, fs, 4,
seed = 55635, cores = 1,
pbest = .25)
output
sel <- mmas(dats, fs, 4, grouping = 'g',
seed = 55635, cores = 1,
pbest = .25)
sel <- mmas(dats, fs, 4, grouping = 'g',
seed = 55635, cores = 1,
pbest = .25)
# minimal example
dats$g <- sample(0:1, nrow(dats), TRUE)
sel <- mmas(dats, fs, 4, grouping = 'g',
seed = 55635, cores = 1,
pbest = .25)
output
lavaan::summary(output)
invariance
?mmas
sel <- mmas(dats, fs, 4, grouping = 'g',
seed = 55635, cores = 1,
pbest = .25)
ordinal
tmp.inv
tmp.inv
cat(input)
cat(input)
long.equal
sel <- mmas(dats, fs, 4, grouping = 'g',
seed = 55635, cores = 1,
pbest = .25)
